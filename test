import os
from pathlib import Path

# 1. First, modify your OUTPUT_EXCEL definition at the top of your script:
OUTPUT_DIR = Path("C:/performance_testing/results")  # ← Change this to your desired directory
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)  # Creates directory if it doesn't exist
OUTPUT_EXCEL = OUTPUT_DIR / f"API_Test_Results_{datetime.now().strftime('%Y%m%d_%H%M%S')}.xlsx"
